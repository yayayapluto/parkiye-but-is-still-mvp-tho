services:

  # ── PostgreSQL ──────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: parkieee_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER:     ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB:       ${DB_NAME:-parkieee}
      TZ:                Asia/Jakarta
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres} -d ${DB_NAME:-parkieee}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - parkieee_net

  # ── Observability stack (uncomment for non-MVP) ────────────────────────────
  # prometheus:
  #   image: prom/prometheus:latest
  #   container_name: parkieee_prometheus
  #   ports:
  #     - "9090:9090"
  #   volumes:
  #     - ./docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
  #   networks:
  #     - parkieee_net

  # grafana:
  #   image: grafana/grafana:latest
  #   container_name: parkieee_grafana
  #   ports:
  #     - "3000:3000"
  #   networks:
  #     - parkieee_net

  # loki:
  #   image: grafana/loki:latest
  #   container_name: parkieee_loki
  #   ports:
  #     - "3100:3100"
  #   networks:
  #     - parkieee_net

  # tempo:
  #   image: grafana/tempo:latest
  #   container_name: parkieee_tempo
  #   ports:
  #     - "3200:3200"
  #   networks:
  #     - parkieee_net

volumes:
  postgres_data:
    driver: local
  pgadmin_data:
    driver: local

networks:
  parkieee_net:
    driver: bridge